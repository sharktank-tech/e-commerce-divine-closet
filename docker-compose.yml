services:
  db:
    image: postgres:16-alpine
    container_name: divine_closet_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: divine
      POSTGRES_PASSWORD: divine
      POSTGRES_DB: divine_closet
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U divine -d divine_closet"]
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: minio/minio:latest
    container_name: divine_closet_minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  minio:
